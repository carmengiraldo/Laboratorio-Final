import { SocioValidatorDirective } from './socio-validator.directive';

describe('SocioValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new   SocioValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
